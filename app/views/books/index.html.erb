<h1>Mis Reservas</h1>
<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Fechas</th>
      <th>Cantidad de personas</th>
      <th>Estado</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.id %></td>
        <td><%= book.start_date %> - <%= book.end_date %></td>
        <td><%= book.guests %></td>
        <td><%= book.status %></td>
        <td>
          <%= link_to "Ver", book_path(book) %>
          <%#= link_to "Editar", edit_book_path(book) %>
          <%= link_to "Cancelar", book_path(book), method: :delete, data: { confirm: "¿Estás seguro de eliminar esta reserva?" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
