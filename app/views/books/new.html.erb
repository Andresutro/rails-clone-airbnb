<h1>Nueva Reserva</h1>

<%= form_with(model: @book, url: books_path) do |form| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Fecha_de_llegada %>
    <%= form.date_field :Fecha_de_llegada %>
  </div>

  <div class="field">
    <%= form.label :Fecha_de_salida %>
    <%= form.date_field :Fecha_de_salida %>
  </div>

  <div class="field">
    <%= form.label :Huéspedes %>
    <%= form.number_field :Huéspedes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
