
<div class="container">
  <div class="row">
  <% @housings.each do |housing| %>
    <div class="col-4 mt-5">
      <%= render "shared/card", housing: housing %>
      <%= link_to housing.name, housing_path(housing) %>
    </div>
  <% end %>
  </div>
</div>
